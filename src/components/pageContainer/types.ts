import {
    ViewStyle
} from "react-native";

interface IPageContainerProps {
    contentContainerStyle?: ViewStyle;
    scrollable?: boolean;
    style?: ViewStyle;
};
export default IPageContainerProps;
