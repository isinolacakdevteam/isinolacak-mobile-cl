import {
    StyleSheet
} from "react-native";

const stylesheet = StyleSheet.create({
    seperator: {
        borderRadius: 2,
        width: "100%",
        height: 1
    }
});
export default stylesheet;
